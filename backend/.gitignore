node_modules
dist
*.log

# Runtime database files at the backend root. Anchored with a leading slash —
# an unanchored `data/` also matches src/data/, which holds tracked source.
/data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

.env
.env.local
